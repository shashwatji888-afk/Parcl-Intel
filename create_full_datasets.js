const fs = require('fs');
const path = require('path');

// We have the raw buyers data and listings data
// Let's create buyers_dataset.csv (2000 rows) and listings_dataset.csv (780+ rows)
console.log('Generating complete production datasets...');
